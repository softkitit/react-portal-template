export enum ActionTypes {
  START_LOADING = '@session/START_LOADING',
  FINISH_LOADING = '@session/FINISH_LOADING',
  SET = '@session/SET',
  REMOVE = '@session/REMOVE',
  SET_USER_ID = '@session/SET_USER_ID',
}
